/*var setPosition = function(float xPos, float zPos){
        
    
    
         float maxHeight = groundGeometry.Find().GetComponent<>().heightScale;
         float xPos = object.transform.position.y;
         float zPos = object.transform.position.x;
    
 
   groundGeometry.vertices.forEach(function (vertex) {
            
       
       
        if(vertex.y == zPos & vertex.x == zPos){
              groundGeometry.vertices.set =(xPos, maxHeight, zPos);
            } 
            
      });
    

}*/
 
 